spring.application.name=CommunityProject

server.port=8079

# MySQL 설정
spring.datasource.url=jdbc:mysql://localhost:3306/dima?serverTimezone=UTC&characterEncoding=UTF-8
spring.datasource.username=root
spring.datasource.password=0305
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# JPA 설정
spring.jpa.show-sql=true
spring.jpa.hibernate.ddl-auto=update
spring.jpa.properties.hibernate.format_sql=true

# Thymeleaf 설정
spring.thymeleaf.mode=HTML5
spring.thymeleaf.encoding=UTF-8
spring.thymeleaf.cache=false

# Jackson 설정
spring.jackson.time-zone=Asia/Seoul

# HikariCP 설정 (MySQL 연결 풀)
spring.datasource.hikari.maximum-pool-size=10
spring.datasource.hikari.connection-timeout=30000
spring.datasource.hikari.idle-timeout=600000
spring.datasource.hikari.max-lifetime=1800000
spring.datasource.hikari.pool-name=CommunityProjectHikariCP

# SMTP 설정 (메일 서비스)
spring.mail.host=smtp.gmail.com
spring.mail.port=587
spring.mail.username=rnrdudghg3122@gmail.com
spring.mail.password=bsyz oyxj axyn tayu
spring.mail.properties.mail.smtp.starttls.enable=true
spring.mail.properties.mail.smtp.starttls.required=true
spring.mail.properties.mail.smtp.auth=true
spring.mail.transport.protocol=smtp
spring.mail.debug=true
spring.mail.default.encoding=UTF-8

# 파일 업로드 설정
spring.servlet.multipart.maxFileSize=100MB
spring.servlet.multipart.maxRequestSize=100MB
spring.servlet.multipart.location=C:/Users/hyunseoLee/OneDrive/바탕 화면/comunity-upload

# 한 페이지당 글의 개수
admin.page.pageLimit=10

# DevTools 설정
spring.devtools.livereload.enabled=true
spring.devtools.restart.enabled=true
